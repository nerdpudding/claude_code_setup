# Todo — 2026-05-30 (Opus 4.8 realignment)

Outstanding work after the Opus 4.8 realignment session. Most of the realignment is DONE
(global `~/.claude/`, both skills, the field-test on local-media-gen). What remains is mostly
documenting the change inside THIS repo (its reusable home) and a few deferred decisions.

Full record + handover: `claude_plans/PLAN_opus_4_8_realign.md`.

## Done this session
- [x] Diagnosed why the setup felt worse on 4.7/4.8 (multi-agent research + adversarial verify)
- [x] Rewrote `global_config/CLAUDE.md` to tiered Hard rules / Preferences
- [x] Created `global_config/output-styles/personal-voice.md` (opt-in tone style)
- [x] Modernized `global_config/skills/project-setup/SKILL.md`
- [x] Created `global_config/skills/realign-project/SKILL.md` (the new `/realign`)
- [x] Light-synced README.md, concepts/concept.md, AI_INSTRUCTIONS.md, doc-keeper.md, example_ai_instructions.md
- [x] Applied everything live to `~/.claude/`
- [x] Field-tested `/realign` on local-media-gen (14 changes, verified)

## Still to do — THIS repo's own documentation (user flagged: don't lose this)
- [x] **README.md** — verified end-to-end (2026-05-30 doc-audit): all sections match global_config/ contents; /realign, output-styles/, and Opus 4.8 principles all present and correct.
- [x] **concepts/concept.md** — confirmed: diagram includes realign-project and output-styles/. Stale "(draft)" label on personal-voice and missing custom_plan skill updated (2026-05-30 doc-audit).
- [x] **AI_INSTRUCTIONS.md** (this repo's own) — confirmed: hierarchy tree + skills table include realign-project, custom_plan, output-styles/. All match actual filesystem.
- [x] **roadmap.md** — re-checked and updated (2026-05-30 doc-audit): Sprint 4 items reflecting actual done state.
- [x] Archive the old `todo_2026-02-17.md` to `archive/` — done (2026-05-30 doc-audit).

## Still to do — content / decisions
- [x] Write a first-class `docs/opus_4_8_alignment.md` capturing the 10 alignment principles + the WHY — DONE. File exists and is complete at `docs/opus_4_8_alignment.md`; README links to it.
- [x] Sync `global_config/settings.json` TEMPLATE to new live defaults — DONE: effortLevel `high`,
      `includeCoAuthoredBy: false`, `outputStyle: "Personal Voice"`, `permissions.deny` example;
      removed old `defaultMode: plan` and `model: opus`.
- [x] Decide: wire `personal-voice` output style ON by default — DONE. `outputStyle: "Personal Voice"` set in both live and template settings; tone rules removed from CLAUDE.md (single-homed in the style); takes effect next session.
- [ ] Optional: a Stop/PostToolUse hook to auto-rename saved plan files to `PLAN_<topic>.md`
      (deferred — verify it can't clobber the wrong file before shipping).

## Still to do — commits
- [x] Committed `claude_code_setup` — commit `4c20f43` "major refactor: realign Claude Code setup to Opus 4.8".
- [ ] Review + commit `local-media-gen` realignment (AI_INSTRUCTIONS slim + 6 new docs/, agents,
      lessons_learned, ARCHITECTURE, settings, .gitignore, tracker rename, shadow-memory deletion).

## User to test
- [ ] The plan-mode workflow: plan → save to `claude_plans/PLAN_<name>.md` with "don't implement
      yet" → build later on explicit instruction. (Native plan mode auto-executes on approval —
      this file-first flow is the workaround. Verify it feels right.)
- [x] `personal-voice` output style is now ON by default (`outputStyle` in settings) — active next session.
